---
phase: 01-test-harness-characterization-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/Config.gs, src/Core.gs, package.json]
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "Requiring src/Config.gs from Node returns the real CONFIG object with exact real catalog values intact (umbralMinimo, horariosPorIdioma, formCols, etc.) — not a synthetic fixture"
    - "Requiring src/Core.gs from Node returns the 5 TEST-01 target functions (mapearColumnas, parsearHorarios, determinarNivel, normalizarNombre, construirBuckets) as callable functions, plus their internal helpers"
    - "src/Config.gs and src/Core.gs are byte-for-byte unchanged except for one additive, guarded module.exports block appended at the end of each file — no existing line is modified"
    - "A root package.json exists with a working `test` script wired to `node --test`, and zero dependencies/devDependencies"
  artifacts:
    - path: "src/Config.gs"
      provides: "Guarded dual-runtime module.exports shim exporting the real CONFIG object"
      contains: "typeof module !== 'undefined'"
    - path: "src/Core.gs"
      provides: "Guarded dual-runtime module.exports shim exporting the 5 TEST-01 functions + helpers"
      contains: "module.exports"
    - path: "package.json"
      provides: "npm test entrypoint (node --test), zero dependencies"
      contains: "\"test\": \"node --test\""
  key_links:
    - from: "package.json scripts.test"
      to: "node --test CLI"
      via: "npm test / npm run test"
      pattern: "node --test"
---

<objective>
Add the dual-runtime `module.exports` shim to `src/Config.gs` and `src/Core.gs` (additive-only, inert under Apps Script V8), and create the repo's first `package.json` so `npm test` becomes a working command. This is the interface-defining wave: Plan 02's test file requires both shimmed source files and the `test` script created here.

Purpose: Give Plan 02's characterization tests a real, requirable `CONFIG` object and the 5 real target functions, with zero risk to the existing copy-paste Apps Script deploy workflow (per D-01/D-02/D-03, `.planning/PROJECT.md` Key Decisions).
Output: `src/Config.gs` (+shim), `src/Core.gs` (+shim), `package.json` (new, root).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-harness-characterization-tests/01-CONTEXT.md
@.planning/phases/01-test-harness-characterization-tests/01-RESEARCH.md
@.planning/phases/01-test-harness-characterization-tests/01-PATTERNS.md
</context>

<interfaces>
<!-- Exact current target-file structure the executor needs. No prior export syntax exists anywhere in this repo (CONVENTIONS.md "No exports/imports" rule) — these two shim blocks are the first ever added. -->

From `src/Config.gs` (103 lines total, full file already read during planning):
- Line 11: `const CONFIG = {` opens the object literal.
- Line 103: `};` closes it — this is the file's last line today.
- The shim must be appended strictly AFTER line 103, as new trailing lines.

From `src/Core.gs` (278 lines total, full file already read during planning):
- Line 75: `function mapearColumnas(headers) { ... }`
- Line 126: `function determinarNivel(conoceNivelResp, nivelDeclaradoResp) { ... }`
- Line 149: `function parsearHorarios(celda, idioma) { ... }`
- Line 184: `function construirBuckets(inscripciones) { ... }`
- Line 266: `function normalizarNombre(texto) { ... }`
- Line 242: `function normalizarTexto(texto) { ... }` (internal helper)
- Line 252: `function normalizarIdioma(valor) { ... }` (internal helper)
- Line 259: `function normalizarNivel(valor) { ... }` (internal helper)
- Line 108: `function primeraCeldaNoVacia(row, indices) { ... }` (internal helper)
- Line 232: `function obtenerLabelHorario(idioma, horarioId) { ... }` (internal helper)
- Line 278 (`normalizarNombre`'s closing brace) is the file's last line today. The shim must be appended strictly AFTER line 278.
- Confirmed by direct read: only `CONFIG` and (inside `parsearHorarios`'s unmatched-label branch, line 161) `Logger.log(...)` are the ambient globals these functions reference — no `SpreadsheetApp`/`MailApp`/`HtmlService`/`ScriptApp` calls anywhere in these 10 functions.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Append guarded module.exports shim to src/Config.gs</name>
  <files>src/Config.gs</files>
  <read_first>
    src/Config.gs (full file, lines 1-103 — confirm the object literal's exact closing line before appending)
    .planning/phases/01-test-harness-characterization-tests/01-RESEARCH.md (Pattern 1: "Guarded dual-runtime module.exports shim")
    .planning/phases/01-test-harness-characterization-tests/01-PATTERNS.md ("src/Config.gs" section)
  </read_first>
  <action>
    Append a new trailing block after line 103 (after the `CONFIG` object literal's closing `};`), and only after it — do not touch or reformat any existing line. The block: a guard `if (typeof module !== 'undefined' && module.exports) { ... }` whose body sets `module.exports = { CONFIG };`. Use 2-space indentation matching the rest of the file (per project CLAUDE.md code style). This block is a true no-op under Apps Script V8 per D-03/RESEARCH.md Assumption A1 (no `module` global exists there), and is provably additive-only (existing lines 1-103 remain byte-for-byte unchanged).
  </action>
  <acceptance_criteria>
    grep -c 'module.exports' src/Config.gs | grep -v '^#' equals 1 (exactly one new export block, not a comment).
    git diff src/Config.gs shows only added lines at the end of the file — zero removed/changed lines among lines 1-103.
    `node -e "const c = require('./src/Config.gs'); if (typeof c.CONFIG !== 'object') throw new Error('CONFIG not exported')"` exits 0.
    `node -e "const {CONFIG} = require('./src/Config.gs'); if (CONFIG.umbralMinimo !== 6) throw new Error('wrong value')"` exits 0 (confirms the REAL catalog, not a synthetic fixture, per D-03).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const {CONFIG} = require('./src/Config.gs'); process.exit(CONFIG && CONFIG.umbralMinimo === 6 && CONFIG.horariosPorIdioma && CONFIG.horariosPorIdioma['Alemán'] ? 0 : 1)"</automated>
  </verify>
  <done>src/Config.gs exports the real CONFIG object via a guarded, additive-only shim; existing lines 1-103 are unchanged; requiring the file from Node returns the exact real catalog values.</done>
</task>

<task type="auto">
  <name>Task 2: Append guarded module.exports shim to src/Core.gs and create root package.json</name>
  <files>src/Core.gs, package.json</files>
  <read_first>
    src/Core.gs (full file, lines 1-278 — confirm exact function names/signatures listed in the &lt;interfaces&gt; block above before appending)
    .planning/phases/01-test-harness-characterization-tests/01-RESEARCH.md (Pattern 1, "Code Examples" → minimal root package.json, Open Question #1 and #2)
    .planning/phases/01-test-harness-characterization-tests/01-PATTERNS.md ("src/Core.gs" and "package.json" sections)
  </read_first>
  <action>
    Append a new trailing block after line 278 (after `normalizarNombre`'s closing brace), and only after it — do not touch or reformat any existing line. The block: a guard `if (typeof module !== 'undefined' && module.exports) { ... }` whose body sets `module.exports` to an object exporting exactly these 10 identifiers: the 5 TEST-01 target functions (`mapearColumnas`, `parsearHorarios`, `determinarNivel`, `normalizarNombre`, `construirBuckets`) plus the 5 internal helpers they call (`normalizarTexto`, `normalizarNivel`, `normalizarIdioma`, `primeraCeldaNoVacia`, `obtenerLabelHorario`) — exporting the helpers too is Claude's discretion per CONTEXT.md and RESEARCH.md's Open Question #1 recommendation, giving Plan 02 the option of direct helper-level assertions without forcing every case through 2-hop indirection. Do NOT export `leerRespuestas`, `parsearModalidad`, or `contarPersonasUnicasPorNivel` — out of TEST-01's named scope.
    Then create a new file at the repo root, `package.json`, containing exactly: `name: "inscripciones-idiomas-pucv"`, `version: "1.1.0"` (matches current CHANGELOG.md version), `private: true`, a `description` noting this is test-harness tooling only and production still runs as copy-pasted `.gs` files, `scripts.test: "node --test"`, and `engines.node: ">=20"` (per RESEARCH.md Open Question #2 — `node:test` is stable since Node 20). Declare NO `dependencies` or `devDependencies` keys (D-01/D-02: zero third-party packages this phase).
  </action>
  <acceptance_criteria>
    grep -c 'module.exports' src/Core.gs equals 1 (exactly one new export block).
    git diff src/Core.gs shows only added lines at the end of the file — zero removed/changed lines among lines 1-278.
    `node -e "const c = require('./src/Core.gs'); ['mapearColumnas','parsearHorarios','determinarNivel','normalizarNombre','construirBuckets'].forEach(k => { if (typeof c[k] !== 'function') throw new Error(k + ' missing') })"` exits 0.
    `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0 (valid JSON).
    `node -e "const p = require('./package.json'); if (p.dependencies || p.devDependencies) throw new Error('unexpected deps'); if (p.scripts.test !== 'node --test') throw new Error('wrong test script')"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const c = require('./src/Core.gs'); const p = require('./package.json'); process.exit(typeof c.mapearColumnas === 'function' && typeof c.parsearHorarios === 'function' && p.scripts.test === 'node --test' ? 0 : 1)"</automated>
  </verify>
  <done>src/Core.gs exports the 5 target functions + 5 helpers via a guarded, additive-only shim (existing lines 1-278 unchanged); root package.json exists with zero dependencies and a working `test` script wired to `node --test`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local Node dev tooling (this phase's test runner, `package.json`) → Production Apps Script V8 runtime | The guarded `module.exports` shim is appended to the exact same `.gs` files that are manually copy-pasted into the Apps Script editor per README "🚀 Instalación" step 2 — it is the only place test-only code physically crosses into the production deployment artifact, since there is no build step to strip it out. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `src/Core.gs` / `src/Config.gs` (guarded shim block) | mitigate | Shim is wrapped in `if (typeof module !== 'undefined' && module.exports)`. Apps Script V8 has no `module` global (RESEARCH.md Assumption A1, cross-referenced against `.planning/codebase/STACK.md`'s documented runtime characteristics) — this makes the block provably a no-op in production. This specific claim was not verified by executing inside a live Apps Script project during research (no such environment was reachable), so Plan 02 Task 3 adds an end-of-phase `<human-check>`: paste the shimmed files into the real Apps Script editor and run `onOpen()`/`recalcularPanoramaConAlerta()` once to confirm zero behavior change, closing the residual verification gap before this phase is accepted (ROADMAP.md Success Criterion 3). |
| T-01-02 | Tampering / Elevation of Privilege | `package.json` | accept | Zero `dependencies`/`devDependencies` declared (D-01/D-02) — no `postinstall` script surface, no typosquatting risk. Package Legitimacy Gate is not applicable this phase: RESEARCH.md's "Package Legitimacy Audit" section confirms no packages are installed (`node:test`/`node:assert` are Node built-ins). |
| T-01-03 | Repudiation / unintended production side effect | `src/Core.gs` (5 target functions + 5 helpers) | accept | Confirmed by direct line-by-line read (RESEARCH.md, PATTERNS.md) that none of these 10 functions call `SpreadsheetApp`/`MailApp`/`HtmlService`/`ScriptApp` — only `CONFIG` and, in one branch, `Logger.log()` are referenced — so no test run can trigger a real spreadsheet write or email send. |

No other high/critical-severity threats apply this phase: no new input-validation surface, no auth/session/crypto code is touched, and zero third-party packages are installed (RESEARCH.md "Security Domain" section).
</threat_model>

<verification>
Run both automated `<verify>` commands above from the repo root; both must exit 0. Confirm via `git diff --stat` that only `src/Config.gs`, `src/Core.gs`, and `package.json` (new file) are touched, and that `git diff src/Config.gs src/Core.gs` shows purely additive hunks at the end of each file.
</verification>

<success_criteria>
- `src/Config.gs` and `src/Core.gs` each gain exactly one guarded `module.exports` block, appended after their current last line, with zero other lines changed.
- `node -e "require('./src/Config.gs').CONFIG.umbralMinimo === 6"` and the 5-function existence check both pass.
- `package.json` exists at repo root, is valid JSON, declares `scripts.test: "node --test"`, and has no `dependencies`/`devDependencies`.
</success_criteria>

<output>
Create `.planning/phases/01-test-harness-characterization-tests/01-01-SUMMARY.md` when done
</output>
