---
phase: 01-test-harness-characterization-tests
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [test/Core.test.js, README.md]
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "Running `npm test` (or `node --test`) from the repo root executes and passes tests for all 5 target functions (mapearColumnas, parsearHorarios, determinarNivel, normalizarNombre, construirBuckets)"
    - "Characterization tests capture, as they behave TODAY: an unmatched/unparseable horario label producing [] from parsearHorarios() (logged via Logger.log, not thrown); Francés falling back to the _default horario catalog entry; an empty/missing header match in mapearColumnas() returning -1"
    - "A short README.md section documents how to run the tests locally, becoming the standard regression check for future edits to these 5 functions"
    - "The guarded module.exports shim (Plan 01) is confirmed inert inside the real Apps Script V8 editor — pasting the shimmed files causes no new errors and identical menu/recalculation behavior (end-of-phase human-check, human_verify_mode=end-of-phase)"
  artifacts:
    - path: "test/Core.test.js"
      provides: "5 describe() blocks characterizing the real, current behavior of mapearColumnas/parsearHorarios/determinarNivel/normalizarNombre/construirBuckets, pinning exact real CONFIG catalog values (D-04)"
      contains: "describe('parsearHorarios'"
    - path: "README.md"
      provides: "New '🧪 Tests' section documenting `npm test` as the standard local regression check"
      contains: "## 🧪 Tests"
  key_links:
    - from: "test/Core.test.js"
      to: "src/Config.gs"
      via: "global.CONFIG = require('../src/Config.gs').CONFIG, set BEFORE requiring Core.gs"
      pattern: "global\\.CONFIG\\s*=\\s*require"
    - from: "test/Core.test.js"
      to: "src/Core.gs"
      via: "require('../src/Core.gs') destructuring the 5 exported target functions"
      pattern: "require\\('\\.\\./src/Core\\.gs'\\)"
    - from: "test/Core.test.js Logger stub"
      to: "parsearHorarios() unmatched-label branch (src/Core.gs:161)"
      via: "global.Logger = { log: () => {} } set BEFORE requiring/invoking Core.gs — without this, the unmatched-horario characterization test throws ReferenceError instead of asserting behavior"
      pattern: "global\\.Logger"
---

<objective>
Write the consolidated characterization test suite (`test/Core.test.js`, per D-05) covering the 5 TEST-01 target functions against Plan 01's real shimmed `CONFIG` and source, add the "🧪 Tests" README section (D-06), and close out ROADMAP Success Criterion 3 with an end-of-phase human-check confirming the shim's production inertness.

Purpose: Establish the regression net every later hardening phase (2-5) depends on — tests must pin the REAL current behavior (including the exact v1.1.0-class bugs already fixed: Alemán hours, missing Italiano entry, Inglés casing), not idealized behavior.
Output: `test/Core.test.js` (new), `README.md` (+"🧪 Tests" section).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-harness-characterization-tests/01-CONTEXT.md
@.planning/phases/01-test-harness-characterization-tests/01-RESEARCH.md
@.planning/phases/01-test-harness-characterization-tests/01-PATTERNS.md
@.planning/phases/01-test-harness-characterization-tests/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Exact real CONFIG values (src/Config.gs) and function behavior (src/Core.gs) the tests must pin, per D-04. Verified by direct read during planning — do not paraphrase these values. -->

Real `CONFIG.horariosPorIdioma` (src/Config.gs:39-55):
- `'Alemán'`: [{ id: 'LM_1730', label: 'Lunes y miércoles (17:30 - 19:30)' }, { id: 'MJ_1730', label: 'Martes y jueves (17:30 - 19:30)' }]
- `'Italiano'`: [{ id: 'MJ_1730_UNICO', label: 'Martes y jueves (17:30 - 19:30) - Único horario disponible este semestre' }]
- `'Inglés'`: [{ id: 'LM_1730', label: 'Lunes y miércoles (17:30 - 19:30)' }, { id: 'VS_1730', label: 'Viernes (17:30 - 19:30) y sábado (10:00 - 12:00)' }]
- `'_default'` (used by `'Francés'`, which has NO dedicated key — src/Config.gs:37-38 inline comment confirms this): [{ id: 'LM_1730', label: 'Lunes y miércoles (17:30 - 19:30)' }, { id: 'VS_1730', label: 'Viernes (17:30 - 19:30) y sábado (10:00 - 12:00)' }]

Real `CONFIG.nivelPrincipiante` = `'A1.1'`, `CONFIG.nivelPorEvaluar` = `'Por evaluar (prueba de nivel)'` (src/Config.gs:60-61).
Real `CONFIG.niveles` includes `'B1.1'` (src/Config.gs:25-29) — needed for a `determinarNivel()` "con exactitud" test case via `normalizarNivel()`.

`mapearColumnas()` (src/Core.gs:75-102): single-index fields (`email`, `idioma`, etc.) use `buscarUno()`, which returns `-1` when neither the exact configured header nor any keyword matches (src/Core.gs:76-80). The `horarios`/`modalidad` fields use `buscarTodos()` (src/Core.gs:82-89), which returns an empty array `[]` (not `-1`) when no column matches that exact question text.

`determinarNivel()` (src/Core.gs:126-140) branches on substring match against `normalizarTexto(conoceNivelResp)`: `'con exactitud'` → `normalizarNivel(nivelDeclaradoResp)`; `'principiante absoluto'` → `CONFIG.nivelPrincipiante`; `'he tomado clases'` → `CONFIG.nivelPorEvaluar`; empty/no match → `''` (never `null`/`undefined`, per CONVENTIONS.md return-value rule).

`normalizarNombre()` (src/Core.gs:266-277): lowercases, splits on whitespace, capitalizes each word EXCEPT lowercase-preserves particles (`'de'`, `'del'`, `'la'`, `'las'`, `'los'`, `'y'`, `'e'`, `'da'`, `'do'`, `'dos'`, `'van'`, `'von'`) when NOT the first word. E.g. `'juan perez de la cruz'` → `'Juan Perez de la Cruz'`.

`construirBuckets()` (src/Core.gs:184-210): keys buckets by `[idioma, nivel, horarioId].join('||')`; each bucket has `count` (increments per matching horario occurrence, can overcount one person across multiple horarios), `emails` (a `Set`, NOT an array — src/Core.gs:197), and `modalidades` (a tally object). Per RESEARCH.md Pitfall 3: use `assert.deepEqual`/`deepStrictEqual` directly on objects containing `Set` values (verified to compare `Set` contents correctly, order-insensitively) — NEVER `JSON.stringify()` a bucket for comparison (a `Set` serializes to `{}`, silently discarding the data under test).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test/Core.test.js — setup, mapearColumnas, parsearHorarios</name>
  <files>test/Core.test.js</files>
  <read_first>
    src/Core.gs (lines 75-102 mapearColumnas, 149-166 parsearHorarios)
    src/Config.gs (lines 39-55 horariosPorIdioma, 80-90 formCols)
    .planning/phases/01-test-harness-characterization-tests/01-RESEARCH.md (Pattern 2: ambient-global stubbing; Pattern 3: characterization tests; Pitfall 1: forgetting Logger stub)
    .planning/phases/01-test-harness-characterization-tests/01-01-SUMMARY.md (confirms exact export names available from Plan 01's shims)
  </read_first>
  <behavior>
    - Setup: `global.CONFIG = require('../src/Config.gs').CONFIG` and `global.Logger = { log: () => {} }` MUST both run before `require('../src/Core.gs')`'s functions are invoked (order matters: CONFIG/Logger must be set before invocation, not necessarily before require() itself, per RESEARCH.md's empirical finding — but set both before any require for clarity and to match the RESEARCH.md Code Examples skeleton verbatim).
    - `mapearColumnas(['MARCA TEMPORAL', 'NOMBRES'])` (no email/idioma header present) -> `cols.email === -1` and `cols.idioma === -1` (buscarUno finds no match).
    - `mapearColumnas(headers)` with headers containing NO column matching CONFIG.formCols.horarios/modalidad exact text -> `cols.horarios` deepEqual `[]` and `cols.modalidad` deepEqual `[]` (buscarTodos returns empty array, not -1 — a distinct empty-value convention from the single-index fields).
    - `mapearColumnas(realHeaders)` where `realHeaders` includes the exact uppercase-trimmed text of every `CONFIG.formCols.*` value -> every returned index is >= 0 (positive-path sanity check pinning today's real header-matching behavior).
    - `parsearHorarios('Un horario inventado que no calza', 'Alemán')` -> `[]` (silently dropped; Logger.log called but stubbed, no throw).
    - `parsearHorarios('Lunes y miércoles (17:30 - 19:30)', 'Francés')` -> `['LM_1730']` (Francés has no dedicated catalog key, per src/Config.gs:37-38; falls back to `_default`, whose first entry has that exact label/id).
    - `parsearHorarios('Lunes y miércoles (17:30 - 19:30), Martes y jueves (17:30 - 19:30)', 'Alemán')` -> `['LM_1730', 'MJ_1730']` (comma-separated multi-select, case/whitespace-insensitive match against the real Alemán catalog).
    - `parsearHorarios('', 'Alemán')` and `parsearHorarios(null, 'Alemán')` -> both `[]` (the `if (!celda) return [];` guard at src/Core.gs:150).
  </behavior>
  <action>
    Create `test/Core.test.js` requiring `node:test` (`test`, `describe`) and `node:assert/strict`. Set `global.CONFIG` from the real, required `Config.gs` export (per D-03 — never a synthetic fixture) and `global.Logger = { log: () => {} }`, then require `../src/Core.gs` and destructure `mapearColumnas` and `parsearHorarios` (plus any other of the 5 target functions this file will also need — Task 2 extends this same file with the remaining 3 describe blocks in one continuous requires-block at the top). Write a `describe('mapearColumnas', ...)` block and a `describe('parsearHorarios', ...)` block implementing every case listed in `<behavior>` above, using `assert.equal`/`assert.deepEqual` per the exact expected values given. Pin the literal real strings/ids shown — do not compute them dynamically from CONFIG inside the test (that would make the test pass even if the catalog silently changed, defeating D-04's purpose).
  </action>
  <verify>
    <automated>node --test test/Core.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    `node --test test/Core.test.js` exits 0 (all assertions in this task's two describe blocks pass).
    grep -c "describe('mapearColumnas'" test/Core.test.js equals 1; grep -c "describe('parsearHorarios'" test/Core.test.js equals 1.
    grep -c "global.Logger" test/Core.test.js is >= 1 (Logger stub present — Pitfall 1 avoided).
    Removing the `global.Logger = { log: () => {} }` line and re-running `node --test test/Core.test.js` reproduces a `ReferenceError: Logger is not defined` on the unmatched-horario test (confirms the stub is load-bearing, not decorative) — restore the line afterward.
  </acceptance_criteria>
  <done>test/Core.test.js exists with working setup (real CONFIG + Logger stub) and passing describe() blocks for mapearColumnas and parsearHorarios covering all <behavior> cases, including the 2 roadmap-mandated edge cases (unmatched horario, Francés fallback).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend test/Core.test.js — determinarNivel, normalizarNombre, construirBuckets</name>
  <files>test/Core.test.js</files>
  <read_first>
    src/Core.gs (lines 126-140 determinarNivel, 184-210 construirBuckets, 266-277 normalizarNombre)
    src/Config.gs (lines 25-29 niveles, 60-61 nivelPrincipiante/nivelPorEvaluar)
    test/Core.test.js (the file created in Task 1 — extend it, do not overwrite the setup/requires block)
  </read_first>
  <behavior>
    - `determinarNivel('Sí, con exactitud y cuento con certificado/curso oficial', 'b1.1')` -> `'B1.1'` (normalizarNivel case-insensitively matches CONFIG.niveles).
    - `determinarNivel('Soy principiante absoluto.', '')` -> `CONFIG.nivelPrincipiante` (`'A1.1'`).
    - `determinarNivel('No, pero he tomado clases.', '')` -> `CONFIG.nivelPorEvaluar` (`'Por evaluar (prueba de nivel)'`).
    - `determinarNivel('', '')` and `determinarNivel(undefined, undefined)` -> both `''` (never null/undefined).
    - `determinarNivel('respuesta que no calza con ninguna rama', '')` -> `''` (fallthrough at src/Core.gs:139).
    - `normalizarNombre('juan perez de la cruz')` -> `'Juan Perez de la Cruz'` (particles 'de'/'la' lowercase-preserved when not first word).
    - `normalizarNombre('  MARÍA   VON TRAPP  ')` -> whitespace-collapsed and title-cased per the real function's exact algorithm (particle 'von' lowercase-preserved) — assert the literal computed expected value by tracing src/Core.gs:266-277 exactly, do not guess.
    - `normalizarNombre('')` -> `''`.
    - `construirBuckets([...])` with 2 synthetic inscripcion objects sharing the same `{idioma, nivel, horarios: [sameHorarioId]}` but different emails -> the resulting bucket's `count === 2`, `emails` is a `Set` of size 2 containing both emails (assert via `assert.deepEqual([...bucket.emails].sort(), [...].sort())` or direct `assert.deepEqual` on the Set per RESEARCH.md Pitfall 3 — NEVER `JSON.stringify`), and `modalidades` tallies each inscripcion's modalidad strings.
    - `construirBuckets([])` -> `{}` (empty object, not null/undefined).
  </behavior>
  <action>
    Extend the SAME `test/Core.test.js` file from Task 1: add `determinarNivel`, `normalizarNombre`, and `construirBuckets` to the existing destructured `require('../src/Core.gs')` import at the top (do not add a second require/setup block — one setup, five describe blocks total per D-05). Add three more `describe()` blocks implementing every case in `<behavior>` above. For the `construirBuckets` tests, build small literal inscripcion-shaped objects inline (`{ idioma, nivel, horarios: [...], modalidades: [...], email, nombre, rowIndex }` — matching the real shape from `leerRespuestas()`, src/Core.gs:50-58) rather than reading from a real sheet. Use `assert.deepEqual`/`deepStrictEqual` for all Set/object comparisons per the Pitfall 3 guidance in `<interfaces>`.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    `npm test` (from repo root, using Plan 01's package.json script) exits 0 with all 5 describe() blocks' tests passing — this is the exact command ROADMAP.md Success Criterion 1 mandates.
    grep -c "describe('determinarNivel'" test/Core.test.js, grep -c "describe('normalizarNombre'" test/Core.test.js, and grep -c "describe('construirBuckets'" test/Core.test.js each equal 1.
    grep -c "describe(" test/Core.test.js equals exactly 5 (one per D-05 — no accidental 6th block, no duplicate setup block).
    `node --test` output (no path argument, from repo root) shows the same pass count as `node --test test/Core.test.js` (confirms `test/` directory auto-discovery works, per RESEARCH.md Pitfall 4).
  </acceptance_criteria>
  <done>test/Core.test.js is complete: one setup block + exactly 5 describe() blocks (mapearColumnas, parsearHorarios, determinarNivel, normalizarNombre, construirBuckets), `npm test` passes for all of them, covering every edge case ROADMAP.md Success Criterion 2 mandates.</done>
</task>

<task type="auto">
  <name>Task 3: Document tests in README.md + end-of-phase inertness verification</name>
  <files>README.md</files>
  <read_first>
    README.md (full file, 95 lines — especially lines 79-92 for placement, lines 44-51 for content-block style)
    .planning/phases/01-test-harness-characterization-tests/01-CONTEXT.md (D-06: README section, not TESTING.md, not inline-only)
    .planning/phases/01-test-harness-characterization-tests/01-PATTERNS.md ("README.md" section — exact heading/placement conventions)
    package.json (created in Plan 01 — confirm exact scripts.test value and engines.node value before documenting them)
  </read_first>
  <action>
    Insert a new `## 🧪 Tests` section into README.md, placed immediately after `## 🔧 Menú` (ends at line 86) and before `## 📞 Contacto` (starts at line 88) — matching the existing emoji + Spanish-title `##` heading convention used by every other section. Content: one short Spanish intro sentence stating this covers the 5 pure `Core.gs` functions (`mapearColumnas`, `parsearHorarios`, `determinarNivel`, `normalizarNombre`, `construirBuckets`) with characterization tests, per TEST-01; a fenced ` ```bash ` block showing `npm test` (no `npm install` needed — zero dependencies) as the run command; a one-line note on the `engines.node` requirement from package.json; and one explicit sentence stating the `module.exports` shim appended to `Core.gs`/`Config.gs` is inert inside the Apps Script editor (no `module` global exists there), so it never affects the copy-paste deploy described in "🚀 Instalación".
  </action>
  <verify>
    <automated>grep -n '## 🧪 Tests' README.md &amp;&amp; grep -n 'npm test' README.md</automated>
    <human-check>
      Deferred to end-of-phase review (workflow.human_verify_mode=end-of-phase): before this phase is marked complete, paste the current contents of `src/Core.gs` and `src/Config.gs` (including the new trailing `module.exports` blocks from Plan 01) into the real Google Apps Script editor bound to the enrollment Sheet — the exact same manual step README's "🚀 Instalación" step 2 already describes. Run `onOpen()` once (or reload the Sheet) and confirm the "🎓 Inscripciones" menu still appears with no execution error. Then run "🔄 Recalcular Panorama" once and confirm it completes without any new error (in particular, no error mentioning `module`) and the "Panorama de Cursos" sheet updates exactly as it did before this phase. This closes ROADMAP.md Phase 1 Success Criterion 3 and the residual verification gap noted in RESEARCH.md Assumption A1 / Plan 01's T-01-01.
    </human-check>
  </verify>
  <acceptance_criteria>
    README.md contains exactly one new `## 🧪 Tests` heading, positioned between the `## 🔧 Menú` and `## 📞 Contacto` headings (confirm via `grep -n '^## '`).
    `git diff README.md` shows only additive lines at the insertion point — no existing section's content is changed.
    The new section's text contains the literal string `npm test` and references `Core.gs`/`Config.gs`'s `module.exports` shim being inert in Apps Script.
  </acceptance_criteria>
  <done>README.md documents `npm test` as the standard local regression check (D-06) in a new "🧪 Tests" section between "🔧 Menú" and "📞 Contacto"; the end-of-phase human-check for Apps Script shim inertness is recorded and deferred per human_verify_mode=end-of-phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local Node dev tooling (`test/Core.test.js`, `npm test`) → Production Apps Script V8 runtime | Same boundary as Plan 01 — the shim this plan's tests exercise is the only crossing point; this plan additionally introduces a `test/` directory, which must never itself cross into the production copy-paste artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 (inherited from Plan 01) | Tampering | `src/Core.gs` / `src/Config.gs` shim | mitigate | Disposition/mitigation unchanged from Plan 01. Task 3's `<human-check>` performs the end-of-phase verification (paste into real Apps Script editor, run `onOpen()`/`recalcularPanoramaConAlerta()`) that closes this out, satisfying ROADMAP.md Success Criterion 3. |
| T-01-04 | Tampering | `test/Core.test.js` | mitigate | The test file lives in a `test/` directory outside `src/`. The existing copy-paste deploy workflow (README "🚀 Instalación" step 2) only ever copies files from `src/` into the Apps Script editor — there is no code path by which this test-only file could be pasted into production. No change to deploy instructions was needed; this follows from the pre-existing folder convention (confirmed in PATTERNS.md — `test/` is a brand-new, clearly separate directory). |
| T-01-05 | Information Disclosure | `test/Core.test.js` (pins real `CONFIG.emailAvisos` = `['idiomas@pucv.cl']`) | accept | This is an existing institutional distribution address already public in `README.md`'s "📞 Contacto" section — pinning it in a local-only test file adds no new disclosure. |

No other high/critical-severity threats apply beyond what Plan 01 already registered — this plan adds no new input surface, no auth/session/crypto code, and no new third-party dependency.
</threat_model>

<verification>
From the repo root: `npm test` must exit 0 and report exactly 5 passing `describe()` groups covering all TEST-01 functions and the 3 roadmap-mandated edge cases (unmatched horario, Francés fallback, missing-header mapearColumnas). `grep -n '## 🧪 Tests' README.md` must find the new section between "🔧 Menú" and "📞 Contacto". The end-of-phase `<human-check>` in Task 3 must be completed (paste-verify in the real Apps Script editor) before this phase is considered fully accepted, per `workflow.human_verify_mode=end-of-phase`.
</verification>

<success_criteria>
- `npm test` (or `node --test`) from the repo root executes and passes tests for all 5 target functions (ROADMAP Success Criterion 1).
- Characterization tests capture the unmatched-horario, Francés→_default, and missing-header edge cases exactly as they behave today (ROADMAP Success Criterion 2).
- The end-of-phase human-check confirms `Core.gs`/`Config.gs` still run unmodified/inert inside the Apps Script V8 editor (ROADMAP Success Criterion 3).
- README.md documents how to run the tests locally (ROADMAP Success Criterion 4).
</success_criteria>

<output>
Create `.planning/phases/01-test-harness-characterization-tests/01-02-SUMMARY.md` when done
</output>
